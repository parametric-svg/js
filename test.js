import "babel/polyfill";

import test from "tape-catch";


test("parametricSVG:  Works with an SVG document", (is) => {
  is.pass("tests work");
  is.end();
  });

test("parametricSVG:  Works with SVG elements", (is) => {
  is.end();
  });

test("parametricSVG:  Works with a VirtualTree", (is) => {
  is.end();
  });

test("parametricSVG:  Creates a new attribute", (is) => {
  is.end();
  });

test("parametricSVG:  Updates an existing attribute", (is) => {
  is.end();
  });

test("parametricSVG:  Changes multiple attributes at once", (is) => {
  is.end();
  });

test("VirtualTree:  Is converted to DOM on .toDOM()", (is) => {
  is.end();
  });

test("VirtualTree:  Is converted to a DOM string on .toString()", (is) => {
  is.end();
  });
